import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const MapScreen = props => {
  return (
    <View>
      <Text>MapScreen</Text>
    </View>
  );
};

export const navOptions = {
  headerTitle: 'Place Detail Screen'
};

const styles = StyleSheet.create({});

export default MapScreen;
